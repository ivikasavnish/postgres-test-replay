# postgres-test-replay
golang based utilities that clone postgres docker from backup or new. create its logical replication layer , save wal inputs to a file. able to scroll through it and apply checkpoint. then able to replay sessions
